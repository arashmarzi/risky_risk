using System;
using System.Collections.Generic;
using RiskAi.Game;
using RiskAi.Game.Agent;
using RiskAi.Game.Land;
using RiskAi.Search.AStar;
using System.Linq;

namespace risky_risk
{
	/// <summary>
	/// Test astar.
	/// </summary>
	public class TestAstar
	{
		/// <summary>
		/// Tests AS tar.
		/// </summary>
		public void TestAStar()
		{
			//const string TERRITORY_FILE_PATH = @"..\..\resources\territories.json";
			//const string CONTINENT_FILE_PATH = @"..\..\resources\continents.json";

			//Board GameBoard = null; // InitializeBoard(TERRITORY_FILE_PATH, CONTINENT_FILE_PATH);
			//List<Player> players;
			//Player player = players[0];

			//var pTerritories = player.ControlledTerritories;


			//var query = from n in pTerritories where (n.Value.Equals(3)) select n.Key;

			//Territory start = GameBoard.Territories.Find(x => x.Name == query.ElementAt(0));
			//Territory finish = GameBoard.Territories.Find(x => x.Name == query.ElementAt(1));

			//AStar astar = new AStar(GameBoard);

			//astar.start(GameBoard, player, start, finish);

			//Console.WriteLine("The path is " + astar.Path);
		}
	}
}
